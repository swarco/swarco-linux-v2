diff -Nru binutils-2.17.50.0.2.orig/configure binutils-2.17.50.0.2/configure
--- binutils-2.17.50.0.2.orig/configure	2006-05-26 18:34:04.000000000 +0200
+++ binutils-2.17.50.0.2/configure	2008-06-05 13:37:57.000000000 +0200
@@ -3647,8 +3647,9 @@
 
     # For an installed makeinfo, we require it to be from texinfo 4.4 or
     # higher, else we use the "missing" dummy.
+    # 2008-06-05 gc: patch regexp to detect makeinfo version >= 4.10
     if ${MAKEINFO} --version \
-       | egrep 'texinfo[^0-9]*([1-3][0-9]|4\.[4-9]|[5-9])' >/dev/null 2>&1; then
+       | egrep 'texinfo[^0-9]*(4\.([6-9]|[1-9][0-9])|[5-9]|[1-9][0-9])' >/dev/null 2>&1; then
       :
     else
       MAKEINFO="$MISSING makeinfo"
diff -Nru binutils-2.17.50.0.2.orig/configure.in binutils-2.17.50.0.2/configure.in
--- binutils-2.17.50.0.2.orig/configure.in	2006-05-26 18:34:04.000000000 +0200
+++ binutils-2.17.50.0.2/configure.in	2008-06-05 13:38:25.000000000 +0200
@@ -2216,8 +2216,9 @@
 changequote(,)
     # For an installed makeinfo, we require it to be from texinfo 4.4 or
     # higher, else we use the "missing" dummy.
+    # 2008-06-05 gc: patch regexp to detect makeinfo version >= 4.10
     if ${MAKEINFO} --version \
-       | egrep 'texinfo[^0-9]*([1-3][0-9]|4\.[4-9]|[5-9])' >/dev/null 2>&1; then
+       | egrep 'texinfo[^0-9]*(4\.([6-9]|[1-9][0-9])|[5-9]|[1-9][0-9])' >/dev/null 2>&1; then
       :
     else
       MAKEINFO="$MISSING makeinfo"
